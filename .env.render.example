# Production Environment Variables for Render Deployment
# Copy these to your Render service environment variables

# Required: Your Gemini API key from Google AI Studio
# Get it from: https://makersuite.google.com/app/apikey
GEMINI_API_KEY=your_gemini_api_key_here

# Application Environment
NODE_ENV=production

# Server Port (Render sets this automatically, but included for reference)
PORT=10000

# Security Note: 
# - Never commit the actual API key to your repository
# - Set these variables in the Render dashboard under "Environment"
# - Keep your API key secure and rotate it regularly
